!!! 5
/ TODO: langの部分を動的に
%html{ lang: 'ja' }
  %head
    %meta{ charset: 'utf-8' }
  %body
    %ul
      - @comments.each do |comment|
          %li { data-id: comment.id }
            = comment.body
